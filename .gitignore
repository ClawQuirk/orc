node_modules/
dist/
.terminal-session.json
.terminal-session.json.tmp
*.log

# Environment (machine-specific ports)
.env

# Runtime data (database, encrypted vault, browser contexts)
data/

# Claude Code local settings (contains user-specific paths/permissions)
.claude/settings.local.json
